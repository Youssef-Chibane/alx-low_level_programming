#include <stdio.h>

/**
* main - entry function
* Description: print all possible combinations of single-digit numbers
* Return: 0 if exited properly
*/

int main(void)
{
	int x;

	for (x = 48; x < 58; x++)
	{
		putchar(n);
		if (x != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
