#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @x : integer to add
 * @y : integer to add
 * Return: the result
*/

int add(int x, int y)
{
	return (x + y);
}
