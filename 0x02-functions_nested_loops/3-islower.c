#include "main.h"

/**
 * _islower - return 1 if the character is lowercase
 * @c: the character that will be checked
 * Return: 0 if the character is not lowercase
*/

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
